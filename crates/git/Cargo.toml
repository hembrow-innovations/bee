[package]
name = "hive-git"
version = "0.1.1"
edition.workspace = true
license.workspace = true
description = "Git lifecycle via shell-out to git"

[dependencies]
thiserror = "2"

[dev-dependencies]
tempfile = "3"
